#include <iostream.h>
int main() {
    float side, area;
    cout << "Enter the length of the side of the Square: ";
    cin >> side;
    area = side * side;
    cout << "Area of the Square = " << area << endl;
    return 0;
}
